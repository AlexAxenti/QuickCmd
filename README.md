# QuickCmd

Initial contract:

Binary: qc
Commands:
    qc s <name> [-- <cmd>]
    qc r <name>
    qc ls
    qc sh <name>
    qc rm <name>
Storage: 
    %APPDATA$\qc\commands.json
Execution:
    Windows only
    via cmd.exe /C